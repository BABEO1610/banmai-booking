import { config } from '../config/env.js'
export async function getHealth(request, response) { let database = { status: config.dataMode === 'postgres' ? 'checking' : 'not-configured' }; if (config.dataMode === 'postgres') { try { const result = await request.app.locals.pool.query("select current_setting('server_version') as version"); database = { status: 'ok', version: result.rows[0].version } } catch { database = { status: 'down' } } } response.status(database.status === 'down' ? 503 : 200).json({ status: database.status === 'down' ? 'degraded' : 'ok', message: 'Backend đang hoạt động', environment: config.nodeEnv, dataMode: config.dataMode, database, integrations: { payment: config.paymentMode, sms: config.smsMode, sheets: config.sheetsMode } }) }
