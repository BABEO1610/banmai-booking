export async function up(pgm) { pgm.createExtension('btree_gist', { ifNotExists: true }); pgm.createSchema('app', { ifNotExists: true }); pgm.createTable('users', { id: { type: 'uuid', primaryKey: true }, email: { type: 'text', notNull: true }, normalized_email: { type: 'text', notNull: true }, password_hash: { type: 'text' }, role: { type: 'text', notNull: true }, status: { type: 'text', notNull: true }, email_verified_at: { type: 'timestamptz' }, created_at: { type: 'timestamptz', notNull: true, default: pgm.func('now()') } }, { schema: 'app' }); pgm.addConstraint({ name: 'users', schema: 'app' }, 'users_normalized_email_unique', { unique: 'normalized_email' }); pgm.createIndex({ name: 'users', schema: 'app' }, ['role'], { name: 'users_single_admin', unique: true, where: "role = 'ADMIN'" }); for (const table of ['sessions', 'audit_log', 'outbox_events', 'idempotency_keys', 'inbox_events']) pgm.createTable(table, { id: { type: 'uuid', primaryKey: true }, payload: { type: 'jsonb' }, created_at: { type: 'timestamptz', notNull: true, default: pgm.func('now()') } }, { schema: 'app' }) }
export async function down(pgm) { pgm.dropSchema('app', true) }
